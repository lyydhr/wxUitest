var noop = require('./noop');

exports = function exports(fn, cb) {
    cb = cb || noop;

    try {
        cb(null, fn());
    } catch (e) {
        cb(e);
        return;
    }
};

module.exports = exports;
