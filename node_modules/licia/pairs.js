var keys = require('./keys');

exports = function exports(obj) {
    var _keys = keys(obj),
        len = _keys.length,
        ret = Array(len);

    for (var i = 0; i < len; i++) {
        ret[i] = [_keys[i], obj[_keys[i]]];
    }

    return ret;
};

module.exports = exports;
