exports = function exports() {
    var arr = arguments,
        sum = 0;

    for (var i = 0, len = arr.length; i < len; i++) {
        sum += arr[i];
    }

    return sum / len;
};

module.exports = exports;
